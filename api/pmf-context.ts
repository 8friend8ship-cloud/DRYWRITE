const endpoint=process.env.AGENT_CORE_ENDPOINT||process.env.AGENT_MAIL_ENDPOINT;
const token=process.env.AGENT_CORE_TOKEN||process.env.AGENT_MAIL_TOKEN;
export default async function handler(req:any,res:any){if(!endpoint||!token)return res.status(500).json({ok:false,error:'AGENT_CORE_ENDPOINT/TOKEN not configured'});if(req.method==='GET'){const u=new URL(endpoint);u.searchParams.set('action','pmf_context');u.searchParams.set('appId',String(req.query?.appId||'APP_DRYWRITE'));u.searchParams.set('token',token);const r=await fetch(u,{cache:'no-store'});return res.status(r.status).send(await r.text());}if(req.method==='POST'){const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...b,token})});return res.status(r.status).send(await r.text());}return res.status(405).json({ok:false,error:'GET or POST only'});}
